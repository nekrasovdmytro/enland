talk:
    resource: "@TalkBundle/Controller/"
    type:     annotation
    prefix:   /

word:
    resource: "@WordBundle/Controller/"
    type:     annotation
    prefix:   /

app:
    resource: "@AppBundle/Controller/"
    type:     annotation
